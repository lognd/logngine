#include <string>
#include <logngine/uncertainty/hello.h>

namespace logngine::units{

// "Hello, World!" serves as a sanity check to make sure everything builds correctly.
std::string hello() { return "Hello from `logngine::units`!"; }

}